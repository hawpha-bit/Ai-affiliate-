<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Replace Your Workload With AI Agents</title>

  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Tailwind Dark Theme Config -->
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            glass: 'rgba(255,255,255,0.08)'
          },
          boxShadow: {
            glow: '0 0 25px rgba(99,102,241,0.6)'
          }
        }
      }
    }
  </script>
</head>

<body class="bg-black text-white min-h-screen flex flex-col items-center">

<!-- STICKY COUNTDOWN -->
<div class="fixed top-0 w-full bg-indigo-600 text-center text-sm py-2 z-50">
  ⚠️ Free AI Automation Blueprint expires in <span id="timer">15:00</span>
</div>

<!-- HERO -->
<section class="pt-20 px-5 max-w-4xl text-center">
  <h1 class="text-4xl md:text-5xl font-bold leading-tight">
    Replace Your Workload With <span class="text-indigo-400">AI Agents</span><br/>
    Before 2026 Forces Everyone Else To
  </h1>

  <p class="mt-5 text-gray-300 text-lg">
    Learn how solo operators are using AI automation to remove themselves from
    daily work — without coding or hiring teams.
  </p>

  <!-- VIDEO PLACEHOLDER -->
  <div class="mt-8 aspect-video bg-glass backdrop-blur-xl rounded-xl border border-white/10 flex items-center justify-center">
    <span class="text-gray-400">▶ 16:9 AI Explainer Video</span>
  </div>

  <!-- CTA -->
  <button onclick="openModal()" class="mt-8 px-8 py-4 bg-indigo-600 hover:bg-indigo-700 rounded-xl font-semibold shadow-glow">
    Get The Free AI Blueprint
  </button>
</section>

<!-- AFFILIATE CTA -->
<section class="mt-16 px-5 max-w-3xl text-center">
  <h2 class="text-2xl font-bold">Already Serious?</h2>
  <p class="text-gray-400 mt-2">
    Skip the free stuff and access the AI automation software professionals are using.
  </p>

  <a href="https://YOUR-CLOAKED-LINK.com"
     class="inline-block mt-6 px-10 py-4 border-2 border-indigo-500 rounded-xl text-indigo-400 hover:bg-indigo-500 hover:text-white transition">
     Access AI Automation System →
  </a>
</section>

<!-- MODAL -->
<div id="modal" class="hidden fixed inset-0 bg-black/70 flex items-center justify-center z-50">
  <div class="bg-glass backdrop-blur-xl p-8 rounded-xl max-w-md w-full border border-white/10">
    <h3 class="text-xl font-bold">Free AI Automation Blueprint</h3>
    <p class="text-gray-400 text-sm mt-2">Enter your details to unlock.</p>

    <!-- FORM -->
    <form action="https://formspree.io/f/YOUR_FORM_ID" method="POST" class="mt-5 space-y-4">
      <input type="text" name="name" required placeholder="Your Name"
        class="w-full px-4 py-3 rounded bg-black border border-white/20"/>

      <input type="email" name="email" required placeholder="Your Email"
        class="w-full px-4 py-3 rounded bg-black border border-white/20"/>

      <button type="submit"
        class="w-full py-3 bg-indigo-600 rounded font-semibold">
        Send Me The Blueprint
      </button>
    </form>

    <button onclick="closeModal()" class="mt-4 text-sm text-gray-400 underline">
      Close
    </button>
  </div>
</div>

<!-- JS -->
<script>
  function openModal() {
    document.getElementById('modal').classList.remove('hidden');
  }

  function closeModal() {
    document.getElementById('modal').classList.add('hidden');
  }

  // Countdown Timer
  let time = 900;
  const timerEl = document.getElementById('timer');
  setInterval(() => {
    if (time > 0) time--;
    const min = Math.floor(time / 60);
    const sec = time % 60;
    timerEl.textContent = `${min}:${sec.toString().padStart(2, '0')}`;
  }, 1000);
</script>

</body>
</html>
